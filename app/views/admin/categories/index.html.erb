<div class="page-header">
  <%= link_to new_admin_administrator_category_path(@administrator), class: 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-plus"></span>
    Novo Category
  <% end %>
  <h1>Listando admin_categories</h1>
</div>

<div class="table-responsive">
  <table class="table table-striped table-bordered table-hover">
    <thead>
      <tr>
        <th>Nome</th>
        <th></th>
        <th></th>
        <th></th>
      </tr>
    </thead>

    <tbody>
      <%= content_tag_for(:tr, @admin_categories) do |admin_category| %>
        <td><%= admin_category.nome %></td>
        <td><%= link_to 'Mostrar', admin_administrator_category_path(@administrator, admin_category) %></td>
        <td><%= link_to 'Editar', edit_admin_administrator_category_path(@administrator, admin_category) %></td>
        <td><%= link_to 'Remover', admin_administrator_category_path(@administrator, admin_category), method: :delete, data: { confirm: 'Você tem certeza?' } %></td>
      <% end %>
    </tbody>
  </table>
</div>
