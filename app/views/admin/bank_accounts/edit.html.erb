<div class="page-header">
  <%= link_to admin_administrator_bank_accounts_path(@administrator), class: 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Voltar
  <% end %>
  <%= link_to @admin_administrator_bank_account_path(@administrator, @admin_bank_account), class: 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-info-sign"></span>
    Mostrar
  <% end %>
  <h1>Editar admin_bank_account</h1>
</div>

<%= render 'form' %>
